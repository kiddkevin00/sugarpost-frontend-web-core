import keyMirror from 'keymirror';

const referralConstants = keyMirror({
  OPENING_MODAL: null,
  SENDING_EMAIL_TO_REFERRAL: null,
  SEND_EMAIL_TO_REFERRAL_SUCCEED: null,
  SEND_EMAIL_TO_REFERRAL_FAIL: null,
});

export default referralConstants;
